#!/bin/sh
# The fast gates, at commit time — the slow ones (tests, e2e) stay in
# `make check` and CI. Installed via:  git config core.hooksPath githooks
set -e
cd "$(git rev-parse --show-toplevel)"
.venv/bin/ruff check tools tests
.venv/bin/mypy tools tests
.venv/bin/python tools/check_loc.py
