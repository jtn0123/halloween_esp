# GENERATED BY tools/gen_esphome.py — DO NOT EDIT
#
# Source of truth: scenes/scenes.yaml
# Regenerate with:  make generate
#
# Each scene is a script: start its audio file, then step the light cues.
# Timings are deltas between absolute cue times from the source file.


script:
  - id: sfx
    mode: restart
    parameters:
      track: string
    then:
      - text_sensor.template.publish:
          id: current_track
          state: !lambda 'return track;'
      - lambda: |-
          esphome::audio::AudioFile *f = nullptr;
          if (track == "01_vigil") f = id(snd_vigil);
          else if (track == "02_storm") f = id(snd_storm);
          else if (track == "03_seance") f = id(snd_seance);
          else if (track == "04_ballroom") f = id(snd_ballroom);
          else if (track == "05_descent") f = id(snd_descent);
          else if (track == "06_visitation") f = id(snd_visitation);
          else if (track == "07_approach") f = id(snd_approach);
          else if (track == "08_crypt") f = id(snd_crypt);
          if (f != nullptr) id(castle_media)->play_file(f, true, false);
          else ESP_LOGW("castle", "no embedded audio for '%s'", track.c_str());
