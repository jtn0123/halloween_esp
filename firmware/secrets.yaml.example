# Copy to firmware/secrets.yaml (gitignored) and fill in the real values.
# ESPHome reads these through !secret in castle.yaml; nothing else does.
# Blank on purpose: a filled-in example is a hardcoded credential to every
# scanner that reads this repo, and this file is tracked.
wifi_ssid: ""
wifi_password: ""
